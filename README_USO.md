# Este archivo fue reemplazado

La documentacion principal ahora vive en `README.md`.

Los cambios mas importantes son:

- nuevo argumento `--audio`
- salidas por audio en `salidas/<audio-slug>/`
- glosario por corrida
- `diagnostico.json` como manifest principal
